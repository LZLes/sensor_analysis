# Runtime dependencies for the local (localhost) web app (web_app/).
# Kept separate from the top-level requirements.txt so installing the
# Streamlit app is never affected by these, and vice versa — same
# isolation rationale macos_app/requirements-macos.txt already used.
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0
scipy>=1.11.0
matplotlib>=3.7.0

# Not used interactively — web_app/ reuses pure functions straight out of
# modes/solid_state.py, modes/amperometry.py, modes/cyclic_voltammetry.py,
# core/shared_tabs.py, and core/constants.py (fit math, PNG-export
# builders) rather than duplicating them. Those files do `import streamlit
# as st` at module level (even though the specific reused functions never
# call st.*), so importing them at all requires streamlit installed — same
# situation macos_app/requirements-macos.txt already documented.
streamlit>=1.32.0
